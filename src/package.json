{
  "name": "syslog-server",
  "version": "1.0.0",
  "description": "A syslog server that stores logs in Postgres DB and forwards to Splunk",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.3.0",
    "dgram": "^1.0.1",
    "pg": "^8.11.0"
  }
}
